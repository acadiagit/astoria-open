# Local dev: http://localhost:7860 → https://astoria.local (same-origin, auto-TLS)
astoria.local:443 {
    tls internal  # Self-signed cert for local; auto-trusts on Mac
    reverse_proxy localhost:7860
}
