# === CORE WEB ===
fastapi==0.116.1
gunicorn==23.0.0
uvicorn[standard]==0.32.0

# === LANGCHAIN (locked to the last fully py3.9-compatible release train) ===
langchain==0.2.16
langchain-core==0.2.41
langchain-community==0.2.16
langchain-groq==0.1.9

# === Google Vertex AI - the only combination that works reliably on py3.9 ===
langchain-google-vertexai==1.0.10
google-cloud-aiplatform==1.70.0
google-auth==2.35.0
google-api-core==2.21.0

# === Vector DB ===
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pgvector==0.3.0
supabase==2.7.1

# === Embeddings - the torch version is the killer on py3.9 ===
torch==2.3.1+cpu ; platform_system == "Linux" and platform_machine == "x86_64"
torchaudio==2.3.1+cpu ; platform_system == "Linux" and platform_machine == "x86_64"
torchvision==0.18.1+cpu ; platform_system == "Linux" and platform_machine == "x86_64"
sentence-transformers==3.1.1

# === Utilities ===
python-dotenv==1.0.1
psutil==6.1.0
